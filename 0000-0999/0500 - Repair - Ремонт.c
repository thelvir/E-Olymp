#include <stdio.h>
#include <stdlib.h>

/* run this program using the console pauser or add your own getch, system("pause") or input loop */

int main(int argc, char *argv[]) {
	long a ,b ,x ,y, c , m , n, l;
	
	scanf("%d",&y );
	for(y=y ; y>0 ; y-=1){
	
	scanf("%ld" "%ld" "%ld",&a,&b,&c );
	x = (2*(a+b))*c;
	printf("%ld\n", (x+15)/16);
}
	
	return 0;
}
